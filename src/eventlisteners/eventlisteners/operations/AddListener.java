package eventlisteners.eventlisteners.operations;

import eventlisteners.ButtonListener;

import javax.swing.*;

public class AddListener extends ButtonListener {

    public AddListener(JTextField numberField) {
        super(numberField);
    }
}
