package eventlisteners.eventlisteners.operations;

import eventlisteners.ButtonListener;

import javax.swing.*;

public class DivideListener extends ButtonListener {
    public DivideListener(JTextField numberField) {
        super(numberField);
    }
}
