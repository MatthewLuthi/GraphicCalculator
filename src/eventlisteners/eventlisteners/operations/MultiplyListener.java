package eventlisteners.eventlisteners.operations;

import eventlisteners.ButtonListener;

import javax.swing.*;

public class MultiplyListener extends ButtonListener {

    public MultiplyListener(JTextField numberField) {
        super(numberField);
    }
}
